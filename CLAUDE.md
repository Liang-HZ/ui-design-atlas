# ui-design-atlas

前端设计原子表 —— 给完全不懂前端的人建立心智的一份可交互参考。线上：https://ui.liangai.org

**部署、域名、统计、故障排查一律先读 [`docs/RUNBOOK.md`](docs/RUNBOOK.md)。**

## 三件最容易踩的事

1. **`public/` 是手写源文件，不是编译产物。** 没有构建步骤，直接改。
   （和 `skills-hub` 相反——那个仓库的 `site/` 会被 CI 覆盖。）

2. **验证域名时不能只看 HTTP 200。** 该域配了泛解析，未正确绑定的子域名不会 404，
   而是返回另一个服务的页面（同样是 200）。必须断言页面里有本站独有的字符串，见 RUNBOOK 第四节。

3. **这份表不承诺模型行为。** 写文案时只描述"输入的性质"（歧义空间大/小），
   不写"模型就会照做/一次做对"这类效果保证。这是产品定位，不是措辞洁癖。

## 改内容之后要自检

表里讲的规则，它自己要遵守：明暗双主题对比度 ≥ 4.5:1、1280 与 375 两个尺寸下页面无横向滚动、
无控制台报错、交叉引用无断链。本地预览 `python3 -m http.server 8899 --directory public`。
